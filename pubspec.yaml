name: project_ui
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  carousel_slider: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true

  # Declare the font
  fonts:
    - family: Sport
      fonts:
#        - asset: assets/fonts/SF-Sports-Night.ttf
        - asset: assets/fonts/SF-Sports-Night-NS-Upright.ttf
          weight: 300
